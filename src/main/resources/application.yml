server:
  port: 9009
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.138.139:5432/icei_employees
    username: sqluser
    password: D3v310p34202x-GD
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true
springdoc:
  packages-to-scan: 'tech.icei.web.api.appuno.controller'
logging:
  pattern:
    console: "%d{dd-MM-yyyy HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"
    file: "%d{dd-MM-yyyy HH:mm:ss.SSS} %p %c{1.} [%t] %m%n"
  level:
    org.springframework.web: ERROR
    tech.icei.web.api.appuno: DEBUG
    org.hibernate: ERROR
  file:
    name: icei-app-employees.log